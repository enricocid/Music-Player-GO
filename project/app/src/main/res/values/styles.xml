<resources>

    <!-- SHEETS/DIALOGS -->
    <!-- https://bryanherbst.com/2020/05/04/bottom-sheet-corners/ -->
    <style name="ThemeOverlay.BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.BottomSheet</item>
    </style>

    <style name="Widget.BottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.BottomSheet</item>
        <item name="backgroundTint">@color/main_bg</item>
    </style>

    <style name="ShapeAppearanceOverlay.BottomSheet" parent="">
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeTopRight">16dp</item>
        <item name="cornerFamily">rounded</item>
    </style>

    <style name="ThemeOverlay.MaterialDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="materialAlertDialogTitleTextStyle">@style/MaterialAlertDialog.App.Title.Text</item>
        <item name="alertDialogStyle">@style/MaterialAlertDialog.App</item>
    </style>

    <style name="MaterialAlertDialog.App" parent="MaterialAlertDialog.Material3">
        <item name="shapeAppearance">@style/ShapeAppearance.App.MediumComponent</item>
        <item name="shapeAppearanceOverlay">@null</item>
    </style>

    <style name="ShapeAppearance.App.MediumComponent" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerSize">@dimen/md_corner_radius</item>
    </style>

    <style name="ThemeOverlay.Settings.MaterialDialog" parent="ThemeOverlay.Material3.Dialog.Alert">
        <item name="dialogCornerRadius">@dimen/md_corner_radius</item>
        <item name="colorBackgroundFloating">@color/main_bg</item>
    </style>

    <!-- Title -->
    <style name="MaterialAlertDialog.App.Title.Text" parent="MaterialAlertDialog.Material3.Title.Text">
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Headline6</item>
    </style>

    <!-- BUTTONS -->
    <style name="Widget.App.CompoundButton.Switch" parent="Widget.Material3.CompoundButton.Switch">
        <item name="android:thumb">@drawable/switch_thumb_mdc</item>
        <item name="thumbTint">@color/switch_thumb_tint_mdc</item>
        <item name="thumbTintMode">src_in</item>
        <item name="track">@drawable/switch_track_mdc</item>
        <item name="trackTint">@color/switch_track_tint_mdc</item>
    </style>

    <!-- SLIDER TIP -->
    <style name="Widget.App.Slider" parent="Widget.Material3.Slider">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Slider</item>
        <item name="labelStyle">@style/Widget.App.Tooltip</item>
    </style>

    <style name="ThemeOverlay.App.Slider" parent="">
        <item name="colorPrimary">@color/widgetsColor</item>
        <item name="colorOnPrimary">@color/widgetsColor</item>
    </style>

    <style name="Widget.App.Tooltip" parent="Widget.Material3.Tooltip">
        <item name="colorSurface">?attr/main_bg</item>
        <item name="android:textColor">?attr/colorSurface</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- TOOLBARS -->
    <!-- Equalizer toolbar -->
    <style name="ToolbarStyle.Eq" parent="ThemeOverlay.Material3.Toolbar.Surface">
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingLeft">16dp</item>
    </style>

    <style name="ToolbarStyle.Title" parent="TextAppearance.Material3.HeadlineSmall">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ToolbarStyle.Subtitle" parent="TextAppearance.Material3.BodySmall" />

    <!-- SearchView -->
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView.ActionBar">
        <item name="closeIcon">@drawable/ic_close</item>
    </style>

    <!-- ALBUM COVER -->
    <style name="ImageView.Cover" parent="">
        <item name="cornerSize">8dp</item>
    </style>
</resources>
